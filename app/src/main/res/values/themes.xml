<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.BobinaBanking" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="imageButtonStyle">@style/PrimaryImageButton</item>
    </style>
    <style name="PrimaryImageButton" parent="Base.Widget.AppCompat.ImageButton">
        <item name="tint">@color/light</item>
        <item name="backgroundTint">@color/primary</item>
    </style>
    <style name="Theme.BobinaBanking" parent="Base.Theme.BobinaBanking" />
</resources>